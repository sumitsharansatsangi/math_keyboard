name: example
description: Example app showing how to use the math_keyboard package.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.11.0 <4.0.0'
  flutter: '>=3.41.2'

dependencies:
  flutter:
    sdk: flutter

  math_keyboard:
    path: ../

flutter:
  uses-material-design: true
